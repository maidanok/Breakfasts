<beans:beans  xmlns="http://www.springframework.org/schema/security"
              xmlns:beans="http://www.springframework.org/schema/beans"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="
              http://www.springframework.org/schema/beans
              http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
              http://www.springframework.org/schema/security
              http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <!-- Здесь располагаются элементы без префикса security: -->
    <http auto-config="true">
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/admin" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/reception" access="hasRole('RECEPTION')"/>
        <intercept-url pattern="/restaurant" access="hasRole('GARCON')"/>

        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/reception/**" access="hasRole('RECEPTION')"/>
        <intercept-url pattern="/restaurant/**" access="hasRole('GARCON')"/>

        <!--default-target-url="/index"-->
        <form-login
                login-page="/login"
                authentication-success-handler-ref="myAuthenticationSuccessHandler"
                authentication-failure-url="/login?error"
                username-parameter="username"
                password-parameter="password"
        />
        <logout logout-success-url="/login?logout"  />
        <csrf/>
    </http>

    <beans:bean id="myAuthenticationSuccessHandler"
                class="by.luchesa.breakfast.web.security.MySimpleUrlAuthenticationSuccessHandler"/>
<!--
    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="mkyong" password="123456" authorities="ROLE_ADMIN" />
            </user-service>
        </authentication-provider>
    </authentication-manager>
-->
    <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service
                    id="userService"
                    data-source-ref="dataSource"
                               users-by-username-query=
                                       "SELECT username, password, enabled FROM users WHERE username=?;"
                               authorities-by-username-query=
                                       "SELECT username, role FROM users, user_roles WHERE users.user_role_id=user_roles.user_role_id AND username=?;" />
        </authentication-provider>
    </authentication-manager>
</beans:beans>
